---
phase: 02-base-modules
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - "Netro Sprinklers.indigoPlugin/Contents/Server Plugin/plugin.py"
  - "tests/test_base_modules.py"
autonomous: true

must_haves:
  truths:
    - "plugin.py imports from constants, exceptions, and utils modules"
    - "Plugin loads without import errors in Python"
    - "All existing functionality preserved (no behavior change)"
    - "Unit tests exist for all three extracted modules"
    - "Pylint score remains >= 9.0"
  artifacts:
    - path: "Netro Sprinklers.indigoPlugin/Contents/Server Plugin/plugin.py"
      provides: "Updated imports from extracted modules"
      contains: "from constants import"
      min_lines: 1500
    - path: "tests/test_base_modules.py"
      provides: "Unit tests for constants, exceptions, utils"
      contains: "def test_"
      min_lines: 80
  key_links:
    - from: "plugin.py"
      to: "constants.py"
      via: "import statement"
      pattern: "from constants import"
    - from: "plugin.py"
      to: "exceptions.py"
      via: "import statement"
      pattern: "from exceptions import"
    - from: "plugin.py"
      to: "utils.py"
      via: "import statement"
      pattern: "from utils import"
    - from: "test_base_modules.py"
      to: "all three modules"
      via: "pytest imports"
      pattern: "from.*constants|exceptions|utils"
---

<objective>
Update plugin.py to import from the extracted modules and create comprehensive unit tests.

Purpose: Complete the module extraction by wiring plugin.py to use the new modules, proving the multi-file pattern works with Indigo. Unit tests verify the extracted code works correctly in isolation.

Output: Updated plugin.py with clean imports and new test_base_modules.py with tests for all three extracted modules.
</objective>

<execution_context>
@/Users/simon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-base-modules/02-01-SUMMARY.md
@Netro Sprinklers.indigoPlugin/Contents/Server Plugin/plugin.py
@Netro Sprinklers.indigoPlugin/Contents/Server Plugin/constants.py
@Netro Sprinklers.indigoPlugin/Contents/Server Plugin/exceptions.py
@Netro Sprinklers.indigoPlugin/Contents/Server Plugin/utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update plugin.py imports to use extracted modules</name>
  <files>Netro Sprinklers.indigoPlugin/Contents/Server Plugin/plugin.py</files>
  <action>
Modify plugin.py to import from the new modules and remove the now-duplicated code.

**Step 1: Add imports after existing imports (around line 47):**
```python
# Import from extracted modules
from constants import (
    NETRO_API_VERSION,
    API_URL,
    MAX_ZONE_DURATION_SECONDS,
    DEFAULT_API_TIMEOUT_SECONDS,
    MINIMUM_POLLING_INTERVAL_MINUTES,
    DEFAULT_WEATHER_UPDATE_INTERVAL_MINUTES,
    THROTTLE_LIMIT_MINUTES,
    FORECAST_UPDATE_INTERVAL_MINUTES,
    DEVICE_INFO_ENDPOINT,
    DEVICE_SCHEDULES_ENDPOINT,
    DEVICE_MOISTURES_ENDPOINT,
    DEVICE_SENSOR_DATA_ENDPOINT,
    DEVICE_WATER_ENDPOINT,
    DEVICE_STOP_WATER_ENDPOINT,
    DEVICE_SET_STATUS_ENDPOINT,
    DEVICE_NO_WATER_ENDPOINT,
    DEVICE_REPORT_WEATHER_ENDPOINT,
    ZONE_START_ENDPOINT,
    OPERATIONAL_ERROR_EVENTS,
    COMM_ERROR_EVENTS,
)
from exceptions import ThrottleDelayError
from utils import convert_timestamp, get_key_from_dict
```

**Step 2: Remove duplicated code from plugin.py:**
- Delete lines 50-88 (API constants, defaults, event sets)
- Delete lines 91-98 (ThrottleDelayError class)
- Delete lines 101-133 (convert_timestamp and get_key_from_dict functions)

**Step 3: Update variable references to use new names:**
- `NETRO_MAX_ZONE_DURATION` -> `MAX_ZONE_DURATION_SECONDS`
- `DEFAULT_API_CALL_TIMEOUT` -> `DEFAULT_API_TIMEOUT_SECONDS`
- `MINIMUM_POLLING_INTERVAL` -> `MINIMUM_POLLING_INTERVAL_MINUTES`
- `DEFAULT_WEATHER_UPDATE_INTERVAL` -> `DEFAULT_WEATHER_UPDATE_INTERVAL_MINUTES`
- `THROTTLE_LIMIT_TIMER` -> `THROTTLE_LIMIT_MINUTES`
- `FORECAST_UPDATE_INTERVAL` -> `FORECAST_UPDATE_INTERVAL_MINUTES`
- `DEVICE_INFO_URL` -> `DEVICE_INFO_ENDPOINT`
- `DEVICE_SCHEDULES_URL` -> `DEVICE_SCHEDULES_ENDPOINT`
- `DEVICE_MOISTURES_URL` -> `DEVICE_MOISTURES_ENDPOINT`
- `DEVICE_SENSOR_DATA_URL` -> `DEVICE_SENSOR_DATA_ENDPOINT`
- `DEVICE_WATER_URL` -> `DEVICE_WATER_ENDPOINT`
- `DEVICE_STOP_WATER_URL` -> `DEVICE_STOP_WATER_ENDPOINT`
- `DEVICE_SET_STATUS_URL` -> `DEVICE_SET_STATUS_ENDPOINT`
- `DEVICE_NO_WATER_URL` -> `DEVICE_NO_WATER_ENDPOINT`
- `DEVICE_REPORT_WEATHER_URL` -> `DEVICE_REPORT_WEATHER_ENDPOINT`
- `ZONE_START_URL` -> `ZONE_START_ENDPOINT`
- `ALL_OPERATIONAL_ERROR_EVENTS` -> `OPERATIONAL_ERROR_EVENTS`
- `ALL_COMM_ERROR_EVENTS` -> `COMM_ERROR_EVENTS`

**Step 4: Remove `from dateutil import tz` (moved to utils.py)**

Use search and replace to update all references. The plugin should have fewer lines after this change.

**Do NOT:**
- Change any logic or behavior
- Modify any method signatures
- Change any Indigo callbacks
  </action>
  <verify>
Run from project root:
```bash
cd "Netro Sprinklers.indigoPlugin/Contents/Server Plugin"
python3 -c "import plugin; print('Plugin imports successfully')"
python3 -m pylint plugin.py --fail-under=9.0
```
Verify: No import errors, Pylint score >= 9.0
  </verify>
  <done>plugin.py imports from constants, exceptions, and utils modules with all references updated to new constant names.</done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive unit tests for extracted modules</name>
  <files>tests/test_base_modules.py</files>
  <action>
Create test_base_modules.py with tests for all three extracted modules.

**File location:** `tests/test_base_modules.py` (create tests/ directory if needed)

**Structure:**
```python
"""Unit tests for extracted base modules (constants, exceptions, utils).

Tests verify the foundation modules work correctly in isolation.
"""
import sys
from pathlib import Path
from datetime import datetime
import pytest

# Add Server Plugin directory to path for imports
SERVER_PLUGIN_DIR = Path(__file__).parent.parent / "Netro Sprinklers.indigoPlugin" / "Contents" / "Server Plugin"
sys.path.insert(0, str(SERVER_PLUGIN_DIR))

from constants import (
    API_URL,
    DEVICE_INFO_ENDPOINT,
    MAX_ZONE_DURATION_SECONDS,
    DEFAULT_API_TIMEOUT_SECONDS,
    MINIMUM_POLLING_INTERVAL_MINUTES,
    THROTTLE_LIMIT_MINUTES,
    OPERATIONAL_ERROR_EVENTS,
    COMM_ERROR_EVENTS,
)
from exceptions import (
    NetroError,
    ThrottleDelayError,
    NetroAPIError,
    NetroConnectionError,
    NetroTimeoutError,
)
from utils import convert_timestamp, get_key_from_dict


class TestConstants:
    """Tests for constants.py module."""

    def test_api_url_format(self):
        """API_URL should be properly formatted with version."""
        assert "v1" in API_URL
        assert API_URL.startswith("http://api.netrohome.com")

    def test_device_info_endpoint(self):
        """DEVICE_INFO_ENDPOINT should be full URL."""
        assert DEVICE_INFO_ENDPOINT.startswith(API_URL)
        assert DEVICE_INFO_ENDPOINT.endswith("info.json")

    def test_max_zone_duration(self):
        """MAX_ZONE_DURATION_SECONDS should be 3 hours in seconds."""
        assert MAX_ZONE_DURATION_SECONDS == 10800
        assert MAX_ZONE_DURATION_SECONDS == 3 * 60 * 60

    def test_default_timeout(self):
        """DEFAULT_API_TIMEOUT_SECONDS should be reasonable."""
        assert 1 <= DEFAULT_API_TIMEOUT_SECONDS <= 60

    def test_minimum_polling_interval(self):
        """MINIMUM_POLLING_INTERVAL_MINUTES should be at least 3."""
        assert MINIMUM_POLLING_INTERVAL_MINUTES >= 3

    def test_throttle_limit(self):
        """THROTTLE_LIMIT_MINUTES should be ~1 hour."""
        assert THROTTLE_LIMIT_MINUTES == 61

    def test_operational_error_events_immutable(self):
        """OPERATIONAL_ERROR_EVENTS should be a frozenset."""
        assert isinstance(OPERATIONAL_ERROR_EVENTS, frozenset)
        assert "startZoneFailed" in OPERATIONAL_ERROR_EVENTS

    def test_comm_error_events_immutable(self):
        """COMM_ERROR_EVENTS should be a frozenset."""
        assert isinstance(COMM_ERROR_EVENTS, frozenset)
        assert "personCall" in COMM_ERROR_EVENTS


class TestExceptions:
    """Tests for exceptions.py module."""

    def test_netro_error_base_class(self):
        """NetroError should be catchable as base class."""
        with pytest.raises(NetroError):
            raise ThrottleDelayError("test")

    def test_throttle_delay_error_message(self):
        """ThrottleDelayError should store message."""
        error = ThrottleDelayError("Rate limited")
        assert str(error) == "Rate limited"
        assert error.message == "Rate limited"

    def test_throttle_delay_error_default_message(self):
        """ThrottleDelayError should have default message."""
        error = ThrottleDelayError()
        assert "rate limit" in error.message.lower()

    def test_throttle_delay_error_retry_after(self):
        """ThrottleDelayError should optionally store retry_after."""
        retry_time = datetime.now()
        error = ThrottleDelayError("test", retry_after=retry_time)
        assert error.retry_after == retry_time

    def test_netro_api_error(self):
        """NetroAPIError should store status and error codes."""
        error = NetroAPIError("Server error", status_code=500, error_code=1)
        assert error.status_code == 500
        assert error.error_code == 1

    def test_exception_hierarchy(self):
        """All custom exceptions should inherit from NetroError."""
        assert issubclass(ThrottleDelayError, NetroError)
        assert issubclass(NetroAPIError, NetroError)
        assert issubclass(NetroConnectionError, NetroError)
        assert issubclass(NetroTimeoutError, NetroError)


class TestUtils:
    """Tests for utils.py module."""

    def test_convert_timestamp_basic(self):
        """convert_timestamp should convert ms to datetime."""
        # 2024-02-02 12:00:00 UTC in milliseconds
        timestamp_ms = 1706875200000
        result = convert_timestamp(timestamp_ms)
        assert isinstance(result, datetime)
        assert result.tzinfo is not None  # Should have timezone

    def test_convert_timestamp_returns_local_timezone(self):
        """convert_timestamp should return local timezone datetime."""
        timestamp_ms = 1706875200000
        result = convert_timestamp(timestamp_ms)
        # Result should be timezone-aware
        assert result.tzinfo is not None

    def test_get_key_from_dict_existing_key(self):
        """get_key_from_dict should return value for existing key."""
        data = {"name": "test", "value": 42}
        assert get_key_from_dict("name", data) == "test"
        assert get_key_from_dict("value", data) == 42

    def test_get_key_from_dict_missing_key(self):
        """get_key_from_dict should return 'unavailable from API' for missing key."""
        data = {"name": "test"}
        assert get_key_from_dict("missing", data) == "unavailable from API"

    def test_get_key_from_dict_none_dict(self):
        """get_key_from_dict should return 'unknown error' for None dict."""
        assert get_key_from_dict("key", None) == "unknown error"

    def test_get_key_from_dict_with_default(self):
        """get_key_from_dict should use default when provided."""
        data = {"name": "test"}
        assert get_key_from_dict("missing", data, default="N/A") == "N/A"

    def test_get_key_from_dict_non_dict(self):
        """get_key_from_dict should handle non-dict gracefully."""
        assert get_key_from_dict("key", "not a dict") == "unknown error"
        assert get_key_from_dict("key", 123) == "unknown error"
```

**Run tests to verify:**
```bash
pytest tests/test_base_modules.py -v
```

**Do NOT:**
- Test Indigo-specific functionality (that requires Indigo runtime)
- Mock external services (pure unit tests)
  </action>
  <verify>
Run from project root:
```bash
pytest tests/test_base_modules.py -v
```
Verify: All tests pass (should be 20+ tests).
  </verify>
  <done>tests/test_base_modules.py exists with comprehensive tests for constants, exceptions, and utils modules, all passing.</done>
</task>

<task type="auto">
  <name>Task 3: Create feature branch, commit changes, and verify</name>
  <files>N/A (git operations)</files>
  <action>
Complete the development workflow by committing changes on a feature branch.

**Step 1: Create feature branch (if not already on one):**
```bash
git checkout -b feature/02-base-modules
```

**Step 2: Stage and commit Plan 01 modules (if not already committed):**
```bash
git add "Netro Sprinklers.indigoPlugin/Contents/Server Plugin/constants.py"
git add "Netro Sprinklers.indigoPlugin/Contents/Server Plugin/exceptions.py"
git add "Netro Sprinklers.indigoPlugin/Contents/Server Plugin/utils.py"
git commit -m "feat(02): extract constants, exceptions, and utils modules

- Create constants.py with API URLs, defaults, and event sets
- Create exceptions.py with ThrottleDelayError and base classes
- Create utils.py with timestamp and dictionary helpers
- All modules use type hints and docstrings
- No dependencies on plugin.py (foundation layer)

Closes #XX (if GitHub issue exists for MOD-01, MOD-02, MOD-03)"
```

**Step 3: Commit plugin.py updates:**
```bash
git add "Netro Sprinklers.indigoPlugin/Contents/Server Plugin/plugin.py"
git commit -m "refactor(02): update plugin.py to use extracted modules

- Import constants, exceptions, utils from new modules
- Remove duplicated code (constants, ThrottleDelayError, helpers)
- Update all constant references to new names
- Plugin functionality unchanged"
```

**Step 4: Commit tests:**
```bash
git add tests/test_base_modules.py
git commit -m "test(02): add unit tests for base modules

- TestConstants: API URL, defaults, event sets
- TestExceptions: hierarchy, attributes, messages
- TestUtils: timestamp conversion, dict access
- 20+ tests, all passing"
```

**Step 5: Final verification:**
```bash
# Verify Pylint score
python3 -m pylint "Netro Sprinklers.indigoPlugin/Contents/Server Plugin/plugin.py" --fail-under=9.0
python3 -m pylint "Netro Sprinklers.indigoPlugin/Contents/Server Plugin/constants.py" --fail-under=9.0
python3 -m pylint "Netro Sprinklers.indigoPlugin/Contents/Server Plugin/exceptions.py" --fail-under=9.0
python3 -m pylint "Netro Sprinklers.indigoPlugin/Contents/Server Plugin/utils.py" --fail-under=9.0

# Run all tests
pytest tests/test_base_modules.py -v
```

**Do NOT:**
- Push to remote (leave for user to review)
- Merge to main without user approval
  </action>
  <verify>
Run:
```bash
git log --oneline -5  # Should show 3 new commits
pytest tests/test_base_modules.py -v  # All tests pass
```
Verify: Branch has commits, tests pass, Pylint >= 9.0
  </verify>
  <done>Feature branch created with atomic commits for modules, plugin updates, and tests. Ready for PR review.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Import chain verification:**
   ```bash
   cd "Netro Sprinklers.indigoPlugin/Contents/Server Plugin"
   python3 -c "
   import plugin
   print('Plugin class:', plugin.Plugin)
   print('Import successful!')
   "
   ```

2. **Test suite:**
   ```bash
   pytest tests/test_base_modules.py -v --tb=short
   ```

3. **Pylint on all files:**
   ```bash
   python3 -m pylint "Netro Sprinklers.indigoPlugin/Contents/Server Plugin/"*.py --fail-under=9.0
   ```

4. **Line count verification:**
   ```bash
   wc -l "Netro Sprinklers.indigoPlugin/Contents/Server Plugin/"*.py
   ```
   - constants.py: ~80 lines
   - exceptions.py: ~50 lines
   - utils.py: ~60 lines
   - plugin.py: ~1550 lines (reduced from 1635)
</verification>

<success_criteria>
- [ ] plugin.py imports from constants, exceptions, utils
- [ ] plugin.py has no duplicated constants or helpers
- [ ] All constant references updated to new names
- [ ] tests/test_base_modules.py exists with 20+ tests
- [ ] All tests pass
- [ ] Pylint >= 9.0 for all Python files
- [ ] Feature branch with atomic commits
</success_criteria>

<output>
After completion, create `.planning/phases/02-base-modules/02-02-SUMMARY.md`
</output>
