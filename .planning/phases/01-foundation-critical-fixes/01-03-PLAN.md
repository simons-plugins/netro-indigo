---
phase: 01-foundation-critical-fixes
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "GitHub issues exist for tracking Phase 1 work"
    - "Issues use minimal content approach (title + brief description)"
    - "Commit messages reference issue numbers with auto-close keywords"
  artifacts: []
  key_links:
    - from: "commit messages"
      to: "GitHub issues"
      via: "auto-close keywords"
      pattern: "(Closes|Fixes|Resolves) #\\d+"
---

<objective>
Create GitHub issues to track Phase 1 work and commit all Phase 1 changes with proper issue references.

Purpose: Establish the GitHub workflow pattern (DEV-01 through DEV-05) that will be used throughout the refactoring project. Issues provide traceability; commits close issues automatically.

Output: GitHub issues created, all Phase 1 work committed with issue references.
</objective>

<execution_context>
@/Users/simon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/simon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-critical-fixes/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub issues for Phase 1 work</name>
  <files></files>
  <action>
Create 3 GitHub issues using the `gh` CLI (following the minimal approach from CONTEXT.md):

**Issue 1: Critical exception handling fixes**
```bash
gh issue create \
  --title "Fix silent exception handlers in plugin.py" \
  --body "Replace all bare \`except (Exception,): pass\` patterns with specific exception types and proper logging. Critical: runConcurrentThread dies silently on errors.

Requirements: CRIT-01, CRIT-02, CRIT-03, CRIT-04"
```

**Issue 2: Code quality and Pylint configuration**
```bash
gh issue create \
  --title "Add pyproject.toml and fix code style issues" \
  --body "Create pyproject.toml with Pylint configuration. Convert .format() to f-strings. Remove unused variables. Target: Pylint 9.0+

Requirements: QUAL-06, QUAL-07, QUAL-08, QUAL-09, QUAL-10"
```

**Issue 3: GitHub workflow documentation**
```bash
gh issue create \
  --title "Establish GitHub issue workflow for project" \
  --body "Document commit-to-issue linking pattern. Use auto-close keywords (Closes #, Fixes #) in commits.

Requirements: DEV-01, DEV-02, DEV-03"
```

Record the issue numbers returned by each command for use in Task 2.
  </action>
  <verify>
1. Run `gh issue list` - should show 3 new issues
2. Each issue has title and body as specified
  </verify>
  <done>3 GitHub issues created for Phase 1 requirements</done>
</task>

<task type="auto">
  <name>Task 2: Commit Phase 1 changes with issue references</name>
  <files></files>
  <action>
Stage and commit all Phase 1 changes with proper issue references:

```bash
cd "/Users/simon/vsCodeProjects/Indigo/netro"

# Check current status
git status

# Stage all modified files
git add "Netro Sprinklers.indigoPlugin/Contents/Server Plugin/plugin.py"
git add pyproject.toml

# Create commit with multi-issue closure
# Replace #1, #2, #3 with actual issue numbers from Task 1
git commit -m "$(cat <<'EOF'
fix: Replace silent exception handlers with proper logging

- Fix runConcurrentThread to handle StopThread and log all other exceptions
- Replace 5 bare except (Exception,) handlers with specific types
- Add logger.exception() for full traceback capture
- Create pyproject.toml with Pylint configuration (target: 9.0+)
- Convert .format() calls to f-strings
- Remove unused variable declarations

Closes #1, closes #2, closes #3

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

If the changes from Plans 01 and 02 were committed separately, create a documentation commit instead:
```bash
git commit --allow-empty -m "$(cat <<'EOF'
docs: Complete Phase 1 foundation work

Exception handling, code quality, and GitHub workflow established.

Closes #3

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```
  </action>
  <verify>
1. `git log --oneline -3` shows commit(s) with "Closes #" pattern
2. `gh issue list --state closed` shows the issues as closed (after push)
  </verify>
  <done>Phase 1 changes committed with issue references</done>
</task>

<task type="auto">
  <name>Task 3: Push changes and verify issue auto-close</name>
  <files></files>
  <action>
Push the commits to GitHub and verify issues are automatically closed:

```bash
cd "/Users/simon/vsCodeProjects/Indigo/netro"

# Push to origin
git push origin master

# Wait a moment for GitHub to process
sleep 2

# Verify issues are closed
gh issue list --state closed

# View issue details to confirm they reference the closing commit
gh issue view 1
gh issue view 2
gh issue view 3
```

If issues are not closed:
1. Check commit message format (must use exact keywords: "Closes #N", "Fixes #N", or "Resolves #N")
2. Ensure commits were pushed to the default branch
3. Verify the repository has the correct default branch configured
  </action>
  <verify>
1. `gh issue list --state closed` shows all 3 Phase 1 issues as closed
2. `gh issue view N` shows "Closed by commit" message
  </verify>
  <done>GitHub workflow verified - commits auto-close issues on push</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Issues exist:** `gh issue list --state all` shows 3 Phase 1 issues
2. **Issues closed:** All 3 issues have "closed" state
3. **Commits reference issues:** `git log --oneline | grep -E "(Closes|Fixes) #"` shows commit(s)
4. **Changes pushed:** `git status` shows "nothing to commit, working tree clean"
</verification>

<success_criteria>
- [ ] 3 GitHub issues created for Phase 1 work items
- [ ] All Phase 1 code changes committed
- [ ] Commit messages use auto-close keywords (Closes #N, Fixes #N)
- [ ] Changes pushed to origin/master
- [ ] Issues automatically closed by GitHub
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-critical-fixes/01-03-SUMMARY.md`
</output>
